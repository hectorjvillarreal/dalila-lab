# Fixture archetypes (no-registry variant) — the SLUG-REGISTRY markers are absent.

Prose still mentions `[pure-diffusion-integrator]` and the retired
`[transition-tempo-skeptic]`: if the validator fell back to prose scanning it would
pass here. It must instead fail loudly.
